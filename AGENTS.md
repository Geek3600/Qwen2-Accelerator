# OPT-125M Hardware Accelerator

## 背景
本项目是一个FPGA流水线硬件加速器，用于加速 OPT-125M 模型的推理，主要是两个场景，一个是序列长度26，另一个是序列长度912，目前处于功能验证阶段，时间非常紧张，需要尽全力修复bug。

## 目标
- 本加速器是流水线加速器，一共11级流水线，权重和数据都是双缓冲，用于计算与访存重叠，一层计算时需要并行的读取下一层的权重，实现流水线式的运行。
- 对加速器代码进行全序列pipeline功能验证，要完全跑通912序列pipeline的功能验证，功能验证完成后要使用远端服务器进行综合，布局布线，最后烧写位流。
- 需要验证并最终实现一个功能正确的，能在FPGA上运行的硬件加速器，最终项目验收时需要在VU9P fpga板卡上进行上板运行。
- 最终加速器能支持序列长度26和912的opt125M 12层模型的推理，目前不要求加速器输出结果与原始模型的输出数值完全一致，目前只需要能上板跑通12层的流水线式的推理，不卡死，有有效数据输出即可
- 能完整运行opt125M 12层模型的流水线推理，包括序列长度26和912的推理，

## 相关文档
- 上下文文档：/home/remote/workspace/Qwen2-Accelerator/docs/context_2026-03-27.md，详细上下文文档中的最后几节是最新内容，用于恢复上下文
- 硬件加速器前期设计文档
  - 流水线架构设计文档：/home/remote/workspace/Qwen2-Accelerator/docs/gptarc2.pdf
  - 量化方案文档：/home/remote/workspace/Qwen2-Accelerator/docs/opt_quan.md

## 规则 / 约束
- 代码使用最小实现，最小修改
- 代码保持原有代码的风格，保证代码的可读性
- 在功能验证时仔细分析问题，找出根因，及时修复。
- 每次有新进展，需要及时更新到上下文文档，防止由于会话中断导致上下文丢失
- 使用远端服务器做vcs编译和验证
- 功能验证时必须使用真实数据和权重进行验证，保证输出数据的规模与真实输出一致
- 每次在远端服务器运行时，可将不需要再使用的，旧的进程终止，保证验证环境干净清洁；并确保已经将新内容同步到远端，防止由于重复运行旧文件造成的时间浪费和bug误判
- 每次bug解决时，都需要说明具体是做了什么改动才解决的问题，保证我对于项目代码细节的知情

## 加速器硬件功能验证规则 / 约束
- 在远端服务器上的vcs进行功能验证，在本地进行硬件代码的修改，修改之后将代码同步到远端服务器上进行验证。
- 功能验证时，必须使用真实数据和权重数据进行验证，保证输出数据的规模与真实输出一致，目前我们的验证目标是整个12层模型的流水线推理能上板与逆行，并且不卡死，有有效数据输出即可，不要求输出数据与原始模型的输出数值完全一致。
- 功能验证时，必须使用真实工程中的接口或者模型进行功能验证，保证与真实上板时的DDR AXI接口保持一致，包括数据的读写，权重的读写，以及模型的推理。
- 功能验证时，必须保证是opt125M模型的12层，11级流水线保证流水线式的模型推理运行，双缓冲机制，这些机制必须保证不能做任何简化。
- 必须严格保证功能验证时的运行流程与真实上板跑的流程一致！这样尽可能减少后续上板所需要的调试时间。
- 请注意修改时不要添加过多的资源，因为现在的资源使用量和布局布线已经很拥塞了
- 功能验证一定要保证进行的是真正流水线式的运行，也就是比如：tokenN完成第一级流水线并进入第二级流水线之后，tokenN+1马上开始第一级流水线，而不是等待tokenN完成全部流水线后才开始下一个token
  
## Task Priority Rules
- The current user prompt always has higher priority than `docs/context_2026-03-27.md`.
- CONTEXT.md is for recovery only, not for overriding the current task.
- Do not continue the previous task unless the user explicitly asks to continue it.
- When the user starts a new task, first restate the new task in one sentence and execute only that task.